<Window x:Class="PcTest.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="PVT-X" Height="900" Width="1400"
        Loaded="OnLoaded"
        Icon="pack://application:,,,/AppLogo.png"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        FontFamily="{DynamicResource AppFont}">
    <Window.Resources>
        <FontFamily x:Key="AppFont">Segoe UI Variable, Segoe UI</FontFamily>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1F1F1F"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#5A5A5A"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#1E40AF"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FCFCFC"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#D6D6D6"/>
        <LinearGradientBrush x:Key="WindowBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F5F6F8" Offset="0"/>
            <GradientStop Color="#EEF1F6" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>
        <Style x:Key="CaptionTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CFCFCF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F3F3F3"/>
            <Setter Property="BorderBrush" Value="#CFCFCF"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="22,10"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EAEAEA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DDDDDD"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F7F7F7"/>
            <Setter Property="ColumnHeaderHeight" Value="32"/>
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F1F1F1"/>
                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="Padding" Value="8,0"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RunHistoryRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E6E6E6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <EventSetter Event="PreviewMouseLeftButtonUp" Handler="OnRunHistoryRowInvoked"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Passed">
                    <Setter Property="Background" Value="#E8F5EE"/>
                    <Setter Property="Foreground" Value="#1F4D3A"/>
                    <Setter Property="BorderBrush" Value="#C8E3D3"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Failed">
                    <Setter Property="Background" Value="#FBEAEA"/>
                    <Setter Property="Foreground" Value="#7C1F1F"/>
                    <Setter Property="BorderBrush" Value="#F2C9C9"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Error">
                    <Setter Property="Background" Value="#FFF4E8"/>
                    <Setter Property="Foreground" Value="#7C3114"/>
                    <Setter Property="BorderBrush" Value="#F8DBC3"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2.5*"/>
            <RowDefinition Height="1.5*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="PVT-X | The Next Gen PC Testing Tool" FontSize="26" FontWeight="SemiBold" />

        <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Margin="0,0,12,0">
                <TextBlock Text="Test Root" FontWeight="SemiBold"/>
                <ui:TextBox Text="{Binding TestRoot, UpdateSourceTrigger=PropertyChanged}" MinWidth="400"/>
            </StackPanel>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical">
                <TextBlock Text="Runs Root" FontWeight="SemiBold"/>
                <ui:TextBox Text="{Binding RunsRoot, UpdateSourceTrigger=PropertyChanged}" MinWidth="300"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,8,12,0">
                <ui:Button Content="Discover Tests" Command="{Binding DiscoverCommand}" Appearance="Secondary" Margin="0,0,8,0"/>
                <ui:Button Content="Refresh History" Command="{Binding RefreshHistoryCommand}" Appearance="Secondary"/>
            </StackPanel>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding StatusMessage}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="12,8,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <ui:Card Padding="20" VerticalAlignment="Top">
                <DockPanel>
                    <TextBlock Text="Tests" Style="{StaticResource SectionHeaderStyle}" DockPanel.Dock="Top" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding Tests}" SelectedItem="{Binding SelectedTest}" BorderThickness="0" Background="Transparent">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="Margin" Value="0,0,0,4"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="border" 
                                                    Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#E8F0FE"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#0078D4"/>
                                                    <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Id}" Style="{StaticResource CaptionTextStyle}"/>
                                    <TextBlock Text="{Binding Category}" Style="{StaticResource CaptionTextStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </ui:Card>

            <ui:Card Grid.Column="1" Padding="0" Margin="16,0,0,0" VerticalAlignment="Top">
                <ScrollViewer Padding="20">
                    <StackPanel>
                        <TextBlock Text="{Binding SelectedTest.Name}" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedTest.Id}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{Binding SelectedTest.Category}" FontWeight="SemiBold" Margin="0,0,8,0"/>
                            <TextBlock Text="|" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding SelectedTest.Version}" Margin="0,0,8,0" />
                            <TextBlock Text="|" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding SelectedTest.Privilege}" />
                        </StackPanel>
                        <TextBlock Text="{Binding SelectedTest.Description}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Timeout:" FontWeight="SemiBold" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding SelectedTest.TimeoutSec}" Margin="0,0,12,0"/>
                            <TextBlock Text="Tags:" FontWeight="SemiBold" Margin="0,0,8,0"/>
                            <ItemsControl ItemsSource="{Binding SelectedTest.Tags}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#EEE" Padding="4,2" CornerRadius="3" Margin="0,0,4,0">
                                            <TextBlock Text="{Binding}" />
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                            <ui:Button Content="Run" Command="{Binding RunCommand}" Appearance="Primary" Icon="{ui:SymbolIcon Play24}" Margin="0,0,8,0" />
                            <ui:Button Content="Cancel" Command="{Binding CancelCommand}" Appearance="Secondary" Icon="{ui:SymbolIcon Dismiss24}" />
                        </StackPanel>

                        <TextBlock Text="Parameters" Style="{StaticResource SectionHeaderStyle}" Margin="0,4,0,4"/>
                        <ItemsControl ItemsSource="{Binding Parameters}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock Text="{Binding Label}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Hint}" Style="{StaticResource CaptionTextStyle}"/>
                                        <ui:TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Text="{Binding Definition.Help}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <ui:Card Padding="12" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Last Result" FontWeight="Bold" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding LastResultStatus}" />
                                <TextBlock Text="{Binding LastResultMessage}" TextWrapping="Wrap"/>
                                <TextBlock Text="Run Folder:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding ActiveRunFolder}" TextWrapping="Wrap" />
                            </StackPanel>
                        </ui:Card>
                    </StackPanel>
                </ScrollViewer>
            </ui:Card>
        </Grid>

        <ui:Card Grid.Row="3" Padding="16" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock Text="Run History" Style="{StaticResource SectionHeaderStyle}" DockPanel.Dock="Top" Margin="0,0,0,8"/>
                <DataGrid ItemsSource="{Binding RunHistory}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" RowStyle="{StaticResource RunHistoryRowStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Run Id" Binding="{Binding RunId}" Width="2*"/>
                        <DataGridTextColumn Header="Test Id" Binding="{Binding TestId}" Width="*"/>
                        <DataGridTextColumn Header="Start" Binding="{Binding StartTime}" Width="*" />
                        <DataGridTextColumn Header="End" Binding="{Binding EndTime}" Width="*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                        <DataGridTextColumn Header="Folder" Binding="{Binding RunFolder}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </ui:Card>
    </Grid>
</Window>
