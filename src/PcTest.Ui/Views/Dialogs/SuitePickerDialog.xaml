<ui:FluentWindow x:Class="PcTest.Ui.Views.Dialogs.SuitePickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:PcTest.Ui.ViewModels"
        xmlns:converters="clr-namespace:PcTest.Ui.Resources"
        mc:Ignorable="d"
        Title="Select Test Suites"
        Height="600" Width="700"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        d:DataContext="{d:DesignInstance vm:SuitePickerViewModel}">
    
    <ui:FluentWindow.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </ui:FluentWindow.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Select Test Suites to Add" 
                   FontSize="18" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>
        
        <!-- Search -->
        <ui:TextBox Grid.Row="1" 
                    PlaceholderText="Search test suites..." 
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,12">
            <ui:TextBox.Icon>
                <ui:SymbolIcon Symbol="Search24"/>
            </ui:TextBox.Icon>
        </ui:TextBox>
        
        <!-- Test Suites List -->
        <Border Grid.Row="2" 
                BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                BorderThickness="1" 
                CornerRadius="4">
            <ListView ItemsSource="{Binding FilteredSuites}"
                      SelectionMode="Multiple"
                      x:Name="SuitesListView">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:SelectableSuiteViewModel}">
                        <Grid Margin="8,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <CheckBox Grid.Column="0" 
                                      IsChecked="{Binding IsSelected}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,12,0"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock FontWeight="SemiBold" FontSize="14">
                                    <Run Text="{Binding Name}"/>
                                    <Run Text=" "/>
                                    <Run Text="{Binding Version, StringFormat='v{0}'}" 
                                         Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Id}" 
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           FontSize="12"/>
                                <TextBlock Text="{Binding Description}" 
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           FontSize="11"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding Description, Converter={StaticResource NullToVisibilityConverter}}"/>
                                <TextBlock Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           FontSize="11">
                                    <Run Text="{Binding NodeCount}"/> test cases
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
        
        <!-- Selection Summary -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="{Binding SelectedCount, StringFormat='{}{0} test suite(s) selected'}"
                       VerticalAlignment="Center"/>
            <ui:Button Content="Clear Selection" 
                       Appearance="Secondary"
                       Margin="12,0,0,0"
                       Command="{Binding ClearSelectionCommand}"
                       Visibility="{Binding HasSelection, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <ui:Button Content="Cancel" 
                       Appearance="Secondary"
                       Width="100"
                       Margin="0,0,8,0"
                       Click="CancelButton_Click"/>
            <ui:Button Content="Add Selected" 
                       Appearance="Primary"
                       Width="120"
                       IsEnabled="{Binding HasSelection}"
                       Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
