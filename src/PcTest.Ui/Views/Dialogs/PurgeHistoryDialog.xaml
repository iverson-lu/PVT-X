<ui:FluentWindow x:Class="PcTest.Ui.Views.Dialogs.PurgeHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:PcTest.Ui.ViewModels"
        xmlns:converters="clr-namespace:PcTest.Ui.Resources"
        mc:Ignorable="d"
        Title="Purge History"
        Height="420" Width="520"
        MinHeight="360" MinWidth="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance vm:PurgeHistoryViewModel}">

    <ui:FluentWindow.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </ui:FluentWindow.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Purge History"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Purge runs older than"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <ui:NumberBox Value="{Binding Days}"
                          Minimum="1"
                          Width="90"
                          Margin="8,0"
                          VerticalAlignment="Center"/>
            <TextBlock Text="days"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

        <TextBlock Grid.Row="2"
                   Text="This will permanently remove execution records and all related artifacts."
                   Foreground="{DynamicResource TextTertiaryBrush}"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <Border Grid.Row="3"
                Background="{DynamicResource ControlBackgroundSecondaryBrush}"
                BorderBrush="{DynamicResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource RadiusS}"
                Padding="12">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="Preview"
                               Command="{Binding PreviewCommand}"
                               IsEnabled="{Binding IsPreviewing, Converter={StaticResource InverseBoolConverter}}"/>
                </StackPanel>

                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0"
                               Text="Runs to delete"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1"
                               Text="{Binding PreviewRunCountDisplay}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0"
                               Text="Estimated artifact size"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="1" Grid.Column="1"
                               Text="{Binding PreviewArtifactSizeDisplay}"
                               Margin="0,6,0,0"/>

                    <TextBlock Grid.Row="2" Grid.Column="0"
                               Text="Time range affected"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="2" Grid.Column="1"
                               Text="{Binding PreviewTimeRangeDisplay}"
                               Margin="0,6,0,0"/>
                </Grid>

                <TextBlock Text="No runs match the purge criteria"
                           Foreground="{DynamicResource TextTertiaryBrush}"
                           Margin="0,8,0,0"
                           Visibility="{Binding ShowNoMatches, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Margin="0,16,0,0">
            <TextBlock Text="This action cannot be undone."
                       Foreground="{DynamicResource TextTertiaryBrush}"
                       Margin="0,0,0,12"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel"
                           Appearance="Secondary"
                           Width="110"
                           Margin="0,0,8,0"
                           Click="CancelButton_Click"/>
                <ui:Button Content="Purge Permanently"
                           Appearance="Danger"
                           Width="150"
                           IsEnabled="{Binding CanPurge}"
                           Click="PurgeButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
