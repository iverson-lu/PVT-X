<UserControl x:Class="PcTest.Ui.Views.Controls.EnvVarEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:PcTest.Ui.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance vm:EnvVarEditorViewModel}">
    <Grid>
        <!-- Environment Variables List -->
        <Border Style="{StaticResource EnvEditorListBorderStyle}">
            <Grid>
                <!-- DataGrid for editing key-value pairs -->
                <DataGrid ItemsSource="{Binding Items}"
                         SelectedItem="{Binding SelectedItem}"
                         Style="{StaticResource EnvEditorDataGridStyle}"
                         ColumnHeaderStyle="{StaticResource EnvEditorColumnHeaderStyle}"
                         CellStyle="{StaticResource EnvEditorCellStyle}"
                         RowStyle="{StaticResource EnvEditorRowStyle}">
                    <DataGrid.Columns>
                        <!-- Key Column -->
                        <DataGridTemplateColumn Header="Key" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="12,6,8,6">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBox Grid.Row="0"
                                                Style="{StaticResource EnvEditorKeyTextBoxStyle}"
                                                Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                                Tag="Key"
                                                ToolTip="{Binding Error}"
                                                ToolTipService.IsEnabled="{Binding HasError}"/>
                                        <TextBlock Grid.Row="1"
                                                  Text="{Binding Error}"
                                                  FontSize="{StaticResource FontSizeXS}"
                                                  Foreground="{DynamicResource StatusErrorBrush}"
                                                  Margin="2,4,0,0"
                                                  Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Value Column -->
                        <DataGridTemplateColumn Header="Value" Width="3*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="12,6,8,6">
                                        <TextBox Style="{StaticResource EnvEditorTextBoxStyle}"
                                                Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                Tag="Value"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Delete Column -->
                        <DataGridTemplateColumn Header="" Width="44">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource EnvEditorIconButtonStyle}"
                                            Command="{Binding DataContext.RemoveRowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            ToolTip="Remove"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                        <ui:SymbolIcon Symbol="Delete24"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty state message -->
                <TextBlock Text="No environment variables. Click Add to create one."
                          Foreground="{DynamicResource TextTertiaryBrush}"
                          FontSize="{StaticResource FontSizeS}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="24"
                          TextWrapping="Wrap"
                          TextAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Items.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
