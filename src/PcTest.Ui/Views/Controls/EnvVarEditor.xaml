<UserControl x:Class="PcTest.Ui.Views.Controls.EnvVarEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:PcTest.Ui.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance vm:EnvVarEditorViewModel}">
    <Grid>
        <Border Style="{StaticResource EnvVarEditorListBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column headers -->
                <Grid Grid.Row="0" Margin="12,10,12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Key" Style="{StaticResource EnvVarEditorHeaderTextStyle}"/>
                    <TextBlock Grid.Column="1" Text="Value" Style="{StaticResource EnvVarEditorHeaderTextStyle}"/>
                </Grid>

                <Border Grid.Row="1"
                        Background="{DynamicResource BorderSubtleBrush}"
                        Height="1"/>

                <!-- Environment Variables List -->
                <Grid Grid.Row="2">
                    <ListView ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem}"
                              SelectionMode="Single"
                              Style="{StaticResource EnvVarEditorListViewStyle}"
                              ItemContainerStyle="{DynamicResource EnvVarEditorSelectableListViewItemStyle}">
                        <ListView.Resources>
                            <Style x:Key="EnvVarEditorSelectableListViewItemStyle"
                                   TargetType="ListViewItem"
                                   BasedOn="{StaticResource EnvVarEditorListViewItemStyle}">
                                <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnRowPreviewMouseLeftButtonDown"/>
                            </Style>
                        </ListView.Resources>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="3*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBox Grid.Row="0"
                                                 Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                                 Tag="Key"
                                                 Style="{StaticResource EnvVarEditorKeyTextBoxStyle}"/>
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding Error}"
                                                   FontSize="{StaticResource FontSizeXS}"
                                                   Foreground="{DynamicResource StatusErrorBrush}"
                                                   Margin="2,4,0,0"
                                                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </Grid>
                                    <TextBox Grid.Column="1"
                                             Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                             Tag="Value"
                                             Margin="12,0,12,0"
                                             Style="{StaticResource EnvVarEditorTextBoxStyle}"/>
                                    <ui:Button Grid.Column="2"
                                              Icon="{ui:SymbolIcon Delete24}"
                                              Style="{StaticResource EnvVarEditorIconButtonStyle}"
                                              Command="{Binding DataContext.RemoveRowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              Focusable="False"
                                              IsTabStop="False"
                                              ToolTip="Remove"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Empty state message -->
                    <TextBlock Text="No environment variables. Click Add to create one."
                              Foreground="{DynamicResource TextTertiaryBrush}"
                              FontSize="{StaticResource FontSizeS}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="24"
                              TextWrapping="Wrap"
                              TextAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Items.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
